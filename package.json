{
    "name": "docs",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "generate-docs": "node scripts/generate-docs-list.js",
        "dev": "npm run generate-docs && vite",
        "build": "npm run generate-docs && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "@tabler/icons-preact": "^3.35.0",
        "marked": "^11.1.1",
        "preact": "^10.19.0",
        "preact-router": "^4.1.1"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.9.0",
        "sass": "^1.94.0",
        "vite": "^5.1.0"
    }
}
